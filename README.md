# duplo-pi

As root:

```
git clone https://github.com/rdegraafwhizzkit/duplo-pi.git
cd duplo-pi/
virtualenv -p python3 --no-site-packages venv
. venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
python ws.py
```
